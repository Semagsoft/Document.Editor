Public Class FluentEmoticonGallery
    Public Event Click(img As BitmapSource)

#Region "Small"

    Private Sub EmoticonAngelSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonAngelSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonAngelSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonAngrySmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonAngrySmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonAngrySmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonCoolSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonCoolSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonCoolSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonCryingSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonCryingSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonCryingSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonDevilishSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonDevilishSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonDevilishSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonEmbarrassedSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonEmbarrassedSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonEmbarrassedSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonKissSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonKissSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonKissSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonlaughSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonlaughSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonlaughSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonMonkeySmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonMonkeySmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonMonkeySmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonPlainSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonPlainSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonMonkeySmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonRaspberrySmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonRaspberrySmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonRaspberrySmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSadSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSadSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSadSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSickSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSickSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSickSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSmileBigSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSmileBigSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSmileBigSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSmileSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSmileSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSmileSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSmirkSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSmirkSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSmirkSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSurpriseSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSurpriseSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSurpriseSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonTiredSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonTiredSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonTiredSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonUncertainSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonUncertainSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonUncertainSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonWinkSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonWinkSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonWinkSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonWorriedSmall_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonWorriedSmall.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonWorriedSmall.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

#End Region

#Region "Normal"

    Private Sub EmoticonAngelNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonAngelNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonAngelNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonAngryNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonAngryNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonAngryNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonCoolNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonCoolNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonCoolNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonCryingNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonCryingNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonCryingNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonDevilishNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonDevilishNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonDevilishNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonEmbarrassedNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonEmbarrassedNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonEmbarrassedNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonKissNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonKissNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonKissNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonlaughNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonlaughNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonlaughNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonMonkeyNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonMonkeyNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonMonkeyNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonPlainNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonPlainNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonPlainNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonRaspberryNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonRaspberryNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonRaspberryNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSadNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSadNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSadNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSickNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSickNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSickNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSmileBigNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSmileBigNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSmileBigNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSmileNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSmileNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSmileNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSmirkNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSmirkNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSmirkNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSurpriseNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSurpriseNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSurpriseNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonTiredNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonTiredNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonTiredNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonUncertainNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonUncertainNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonUncertainNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonWinkNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonWinkNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonWinkNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonWorriedNormal_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonWorriedNormal.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonWorriedNormal.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

#End Region

#Region "Large"

    Private Sub EmoticonAngelLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonAngelLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonAngelLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonAngryLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonAngryLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonAngryLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonCoolLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonCoolLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonCoolLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonCryingLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonCryingLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonCryingLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonDevilishLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonDevilishLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonDevilishLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonEmbarrassedLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonEmbarrassedLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonEmbarrassedLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonKissLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonKissLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonKissLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonlaughLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonlaughLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonlaughLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonMonkeyLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonMonkeyLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonMonkeyLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonPlainLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonPlainLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonPlainLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonRaspberryLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonRaspberryLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonRaspberryLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSadLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSadLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSadLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSickLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSickLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSickLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSmileBigLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSmileBigLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSmileBigLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSmileLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSmileLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSmileLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSmirkLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSmirkLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSmirkLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonSurpriseLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonSurpriseLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonSurpriseLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonTiredLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonTiredLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonTiredLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonUncertainLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonUncertainLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonUncertainLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonWinkLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonWinkLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonWinkLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

    Private Sub EmoticonWorriedLarge_MouseDown(sender As Object, e As MouseButtonEventArgs) Handles EmoticonWorriedLarge.MouseDown
        e.Handled = True
        Dim i As BitmapSource = TryCast(EmoticonWorriedLarge.Source, BitmapSource)
        RaiseEvent Click(i)
    End Sub

#End Region

End Class